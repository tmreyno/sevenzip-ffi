[package]
name = "sevenzip-ffi-tauri"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }

[features]
default = []
# Enable when building the real Tauri app:
tauri = []
